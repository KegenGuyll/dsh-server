//#region lib/types/index.js
/** Host loader entry for the browser-only speech-to-text plugin. */
/** Provides no host-side behavior (all transcription runs in the browser). */
function apply() {}
//#endregion
export { apply };
